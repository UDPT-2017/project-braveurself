<h2>User</h2>
<hr>
<table class="table table-hover">
  <thead>
    <tr>
      <th>#</th>
      <th>Name</th>
      <th>Email</th>
      <th>Address</th>
      <th>Edit</th>
      <th>Delete</th>
    </tr>
  </thead>
  <tbody>
    <% normalUser.forEach(function(user, index){ %>
      <tr>
        <th scope="row"><%= index + 1 %></th>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= user.address %></td>
        <td>
            <a href="user/edit/<%= user.id %>">
                <button type="button" class="btn btn-primary btn-sm">Edit</button>
            </a>
        </td>
        <td>
            <a href="user/delete/<%= user.id %>">
                <button type="button" class="btn btn-danger btn-sm">Delete</button>
            </a>
        </td>
      </tr>
      <% }); %>
  </tbody>
</table>

<br>

<h2>Admin</h2>
<hr>
<table class="table table-hover">
  <thead>
    <tr>
      <th>#</th>
      <th>Name</th>
      <th>Email</th>
      <th>Address</th>
      <th>Edit</th>
      <th>Delete</th>
    </tr>
  </thead>
  <tbody>
    <% admin.forEach(function(user, index){ %>
      <tr>
        <th scope="row"><%= index + 1 %></th>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= user.address %></td>
        <td>
            <a href="user/edit/<%= user.id %>">
                <button type="button" class="btn btn-primary btn-sm">Edit</button>
            </a>
        </td>
        <td>
            <a href="user/delete/<%= user.id %>">
                <button type="button" class="btn btn-danger btn-sm">Delete</button>
            </a>
        </td>
      </tr>
      <% }); %>
  </tbody>
</table>